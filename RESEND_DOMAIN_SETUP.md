# 📧 إعداد Domain في Resend لإرسال الإيميلات

## المشكلة الحالية
```
You can only send testing emails to your own email address (rabeh2020ken@gmail.com). 
To send emails to other recipients, please verify a domain at resend.com/domains
```

## الحل: تفعيل Domain

### الخطوة 1: شراء أو استخدام Domain موجود
- إذا كان لديك domain (مثل: `mechamind.com`)
- أو استخدم subdomain مجاني من Vercel

### الخطوة 2: إضافة Domain في Resend

1. **افتح**: https://resend.com/domains
2. **اضغط**: "Add Domain"
3. **أدخل**: Domain الخاص بك (مثل: `mechamind.com`)
4. **احفظ**

### الخطوة 3: إضافة DNS Records

Resend سيعطيك 3 سجلات DNS يجب إضافتها:

#### SPF Record:
```
Type: TXT
Name: @
Value: v=spf1 include:_spf.resend.com ~all
```

#### DKIM Records (2):
```
Type: TXT
Name: resend._domainkey
Value: [قيمة طويلة ستظهر في لوحة Resend]

Type: TXT
Name: resend2._domainkey
Value: [قيمة طويلة ثانية]
```

### الخطوة 4: إضافة السجلات في مزود الـ Domain

**إذا كنت تستخدم Cloudflare:**
1. اذهب إلى: Cloudflare Dashboard → DNS
2. اضغط: "Add Record"
3. أضف السجلات الثلاثة أعلاه

**إذا كنت تستخدم Vercel:**
1. اذهب إلى: Vercel Dashboard → Settings → Domains
2. أضف السجلات في قسم DNS

**إذا كنت تستخدم Namecheap/GoDaddy:**
1. اذهب إلى: لوحة التحكم → Advanced DNS
2. أضف السجلات

### الخطوة 5: التحقق من Domain

1. ارجع إلى: https://resend.com/domains
2. اضغط: "Verify DNS Records"
3. انتظر (قد يستغرق 5-60 دقيقة)
4. عندما يظهر ✅ **Verified** → Domain جاهز!

### الخطوة 6: تحديث الكود

بعد تفعيل Domain، غيّر في `lib/email.ts`:

```typescript
from: 'MechaMind <noreply@mechamind.com>',  // استخدم domain الخاص بك
```

---

## البديل المؤقت (للتطوير فقط)

إذا كنت تريد اختبار الآن بدون Domain:

### الطريقة 1: استخدم البريد المسجل
```typescript
// في app/api/auth/signup/route.ts
const emailToSend = 'rabeh2020ken@gmail.com'; // البريد المسجل في Resend
```

### الطريقة 2: عرض الكود في Console فقط
```typescript
// لا ترسل إيميل، فقط اطبع الكود
console.log('🔐 OTP Code:', code);
// المستخدم يدخل الكود يدوياً
```

---

## خيارات بديلة لـ Resend

إذا كنت لا تريد شراء domain:

### 1. **SendGrid** (مجاني حتى 100 إيميل/يوم)
- https://sendgrid.com
- يعطيك بريد مجاني: `@sendgrid.net`

### 2. **Mailgun** (أول 5000 إيميل مجاناً)
- https://mailgun.com
- يوفر sandbox domain للتجربة

### 3. **Gmail SMTP** (مجاني بالكامل)
- استخدم حساب Gmail
- لكن محدود بـ 500 إيميل/يوم

---

## تطبيق الحل الحالي

الكود الحالي يعمل الآن في وضع التطوير:
- ✅ يرسل الإيميل إلى `rabeh2020ken@gmail.com` فقط
- ✅ يطبع الكود في Console للمستخدمين الآخرين
- ✅ يسمح بإكمال التسجيل حتى لو فشل الإيميل

**للاختبار:**
1. سجل بأي إيميل
2. انظر في Console للحصول على الكود
3. أدخل الكود للتحقق

**للإنتاج:**
- يجب تفعيل Domain كما هو موضح أعلاه ✅
