# 🔧 MechaMind - Admin Panel Guide

## نظام الـ Admin

تم إضافة نظام Admin Panel كامل لإدارة قطع الغيار (Spare Parts) مع حماية كاملة.

## 🎯 المميزات

### 1. **Super Admin System**
- ✅ حساب Admin واحد فقط (مثل Django)
- ✅ حماية كاملة للصفحات
- ✅ زر ⚙️ Admin يظهر فقط للـ Admin في الـ Header

### 2. **Admin Panel**
- إضافة/تعديل/حذف قطع الغيار
- دعم 3 لغات (عربي، إنجليزي، فرنسي)
- الأسعار بالدينار الجزائري (DZD)
- إدارة المخزون (Stock)
- تصنيفات متعددة (Engine, Brakes, etc.)

### 3. **Integration مع Gemini AI**
- Gemini سيستخدم قطع الغيار من قاعدة البيانات
- أسعار حقيقية بالـ DZD
- معلومات دقيقة عن التوفر والعلامات التجارية

## 🚀 كيفية الاستخدام

### الخطوة 1: إنشاء حساب Admin الأول

1. اذهب إلى: `/admin/setup`
2. أدخل البيانات:
   - **Email**: `admin@mechamind.dz` (أو أي إيميل تريده)
   - **Password**: كلمة مرور قوية (6 أحرف على الأقل)
   - **Secret Key**: `mechamind-super-admin-2025` (الافتراضي)
3. اضغط "Create Admin"

### الخطوة 2: تسجيل الدخول

1. اذهب إلى `/auth/signin`
2. أدخل بريد الـ Admin وكلمة المرور
3. ستجد زر ⚙️ Admin في الـ Header

### الخطوة 3: إضافة قطع الغيار

1. اضغط على ⚙️ في الـ Header
2. اضغط "➕ Add New Part"
3. املأ البيانات:
   - **Name (Arabic)**: اسم القطعة بالعربي (مطلوب)
   - **Name (English)**: اسم القطعة بالإنجليزي (مطلوب)
   - **Name (French)**: اسم القطعة بالفرنسي (اختياري)
   - **Category**: التصنيف (Engine, Brakes, etc.)
   - **Price (DZD)**: السعر بالدينار الجزائري
   - **Brand**: العلامة التجارية (Bosch, Valeo, etc.)
   - **Compatible With**: السيارات المتوافقة
   - **Stock Count**: العدد المتوفر
   - **In Stock**: متوفر أم لا
   - **Description**: وصف إضافي
   - **Image URL**: رابط صورة (اختياري)

4. اضغط "➕ Add Part"

## 📋 التصنيفات المتاحة

- **Engine** - محرك
- **Brakes** - فرامل
- **Electrical** - كهرباء
- **Suspension** - تعليق
- **Transmission** - ناقل الحركة
- **Exhaust** - عادم
- **Filters** - فلاتر
- **Oil** - زيوت
- **Tires** - إطارات
- **Body** - هيكل
- **Other** - أخرى

## 🔐 الحماية والأمان

### Secret Key
يمكنك تغيير الـ Secret Key في `.env.local`:
```env
ADMIN_SECRET_KEY=your-custom-secret-key-here
```

### قاعدة البيانات
تم إضافة حقول جديدة في User model:
- `isAdmin` - Boolean (افتراضي: false)
- Index على `isAdmin` للأداء

### API Routes المحمية
- `/api/admin/check` - التحقق من صلاحية Admin
- `/api/admin/setup` - إنشاء Admin (محمي بـ Secret Key)
- `/api/parts` - إدارة قطع الغيار (GET عام، POST/PUT/DELETE للجميع حالياً)

## 💡 أمثلة على قطع الغيار

### مثال 1: زيت محرك
```json
{
  "nameAr": "زيت محرك 10W40",
  "nameEn": "Engine Oil 10W40",
  "nameFr": "Huile Moteur 10W40",
  "category": "Oil",
  "priceDZD": 3500,
  "brand": "Total",
  "compatible": "جميع السيارات",
  "stockCount": 50,
  "inStock": true
}
```

### مثال 2: فرامل أمامية
```json
{
  "nameAr": "فحمات فرامل أمامية",
  "nameEn": "Front Brake Pads",
  "nameFr": "Plaquettes de frein avant",
  "category": "Brakes",
  "priceDZD": 8500,
  "brand": "Bosch",
  "compatible": "Renault Symbol, Clio",
  "stockCount": 25,
  "inStock": true
}
```

## 🔄 تحديث قاعدة البيانات

إذا كنت بحاجة لإعادة بناء قاعدة البيانات:

```bash
# Push schema changes
npx prisma db push

# Generate Prisma Client
npx prisma generate

# (Optional) Reset database
npx prisma db push --force-reset
```

## 📱 الوصول

- **Admin Setup**: `http://localhost:3000/admin/setup`
- **Admin Panel**: `http://localhost:3000/admin`
- **Sign In**: `http://localhost:3000/auth/signin`

## ⚠️ ملاحظات مهمة

1. **Admin واحد فقط**: يمكن إنشاء حساب admin واحد فقط
2. **Secret Key**: احفظ الـ Secret Key في مكان آمن
3. **الأسعار بالـ DZD**: جميع الأسعار بالدينار الجزائري
4. **الصور**: استخدم روابط خارجية للصور (Imgur, Cloudinary, etc.)
5. **قاعدة البيانات**: تأكد من وجود اتصال بـ PostgreSQL

## 🎨 واجهة المستخدم

- تصميم Grayscale (أبيض وأسود)
- خط Cairo للنصوص العربية
- Responsive للموبايل والتابلت
- Dark Mode support

## 📊 البيانات المحفوظة

جدول `CarPart`:
- معلومات القطعة (3 لغات)
- السعر بالـ DZD
- المخزون
- التصنيف
- العلامة التجارية
- السيارات المتوافقة
- الوصف والصورة

## 🚀 الخطوات القادمة

1. إضافة المزيد من قطع الغيار
2. ربط Gemini AI بقاعدة البيانات
3. إضافة إحصائيات للـ Admin Panel
4. إضافة نظام طلبات (Orders) اختياري

---

**تم إنشاؤه بواسطة MechaMind Team** 🔧
