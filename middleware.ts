// Middleware disabled for Static Export (SSG)
// Authentication is handled client-side in each page component
// This allows the app to work as a static export for mobile (Android)

export const config = {
  matcher: [],
}
